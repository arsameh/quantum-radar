### Prerequisites
* **C++ Compiler** with C++17 support
* **CMake**

Run the following commands from the root directory of the project:

```bash
cmake -B build

cmake --build build
```

**Windows:**
```cmd
.\build\Debug\QuantumRadar.exe
```

**Linux / Mac:**
```
./build/QuantumRadar
```